# OpenTofu / Terraform.terraform/.tfstate
.tfstate.
crash.log
crash..log
*.tfvars
*.tfvars.json
tfplanoverride.tf
override.tf.json
*_override.tf
*_override.tf.json

# OpenTofu / Terraform
.terraform/
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
.terraform.lock.hcl

# Python
pycache/
*.py[cod]
*$py.class
cpython-.pyc

## C extensions
*.so

#Distribution / packaging

build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

.venv/
/venv
/ENV/
.env

# SG: generated by `hatch run sbom:run`; CI regenerates it and uploads it as an
# artifact, so the checked-out tree never needs a stale copy.
src/sbom.json

# Dev-workflow cursor: session-local scratch state.
# .ai/next-steps.md IS committed — it is what travels between sessions.
.ai/state.json

# VS Code / OS
.vscode/
.DS_StoreThumbs.db
