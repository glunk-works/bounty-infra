### FILEPATH: /sprints/S0_governance_hardening/sprint_plan.md

**Sprint Goal:** Stop the bleeding on the High workflow/governance findings and install the gate that makes every later fix safe. S0 stands up branch protection + a minimal working method, converts the ungated `push→tofu apply -auto-approve` deploy into a **plan-on-PR + Environment-approved apply** flow (#9), makes CI **non-bypassable** on all delivery paths (#8), and fixes the `run-scan.yml` **script-injection** surface (#6) while dropping the unused `GITHUB_TOKEN` (#10). This is the governance-first sprint (BI-D1); scanner security (#7/#13) is S1, robustness (#11/#12/#14) is S2. **S0 also unblocks loop-orchestrator S47's #18** — the recon dispatch contract adds inputs to the same `run-scan.yml` that #6 hardens.

**Out of scope:**
- **Scanner code (#7 scope, #13 triage).** No `scanner.py` change — S1. S0 is `.github/` + `infra/` CI/CD + repo governance only.
- **Robustness (#11 IAM, #12 pinning, #14 partial-success).** S2.
- **Standing up the central conventions repo (BI-D3) and the compute-model / Ansible decision.** Both are their own passes (see the roadmap). S0's `CLAUDE.md` references loop-orchestrator's `conventions.md` as the **interim** convention source + adds a local bounty-infra section, and re-points at the central repo once it exists. (BI-D3 is docs/conventions only — no shared-code package.)
- **The full method (skills, IaC security-critic agent, `architect-review` CI gate).** Interleaves across S1/S2 (MG1). S0 lands only the *minimal* method: `CLAUDE.md`, the `.ai/` cursor, the ruleset.
- **OPEN — the four unadopted shared gates.** loop-orchestrator requires `secrets-scan`, `dependency-audit`, `sbom`, and `pr-title`; bounty-infra has **none** of them, and no sprint currently schedules them. Deliberately **not** pulled into S0 (it is already carrying four tasks), but they are the visible delta against the shared taxonomy — sequence them explicitly rather than letting them drift. `secrets-scan` is the one with a same-sprint argument, given this repo is public.

**Context — decisions (owner-confirmed via micro-gates, 2026-07-21; see `docs/hardening_roadmap.md`):**
- **BI-D1** governance-first · **BI-D2** gated deploy (plan-on-PR + apply-on-merge + `production` Environment approval; `-auto-approve` runs only post-approval) · **BI-D3** a central repo hosts the shared conventions/way-of-working that each `CLAUDE.md` references + locally extends (docs only, **not** a shared-code package; interim source: loop-orchestrator's `conventions.md`).

**Tasks:**

- **Task 1: Branch protection + minimal working-method scaffold**
  - **Description:** ✅ **Ruleset DONE 2026-07-21.** `protected-integration-branches` (id `19438326`) is active on `refs/heads/main`: `deletion` + `non_fast_forward` + `pull_request`, `bypass_actors: []`. **No approving-review requirement** (`required_approving_review_count: 0`) — the repo has a single collaborator and GitHub forbids self-approval, so the originally-planned `≥1 review` would have made every subsequent S0 PR permanently unmergeable. This matches **loop-orchestrator's ruleset of the same name, field for field**; the human's merge click is the approval, per the working method. Structural protection went on **first** (no check dependency); the **required-status-checks list is finalized at the END of S0** (T2/T3 create the checks — a required check that does not yet exist strands every PR). ✅ **Scaffold DONE 2026-07-21.** A lean `CLAUDE.md` (routing layer, referencing loop-orchestrator's `conventions.md` as the interim shared source + a bounty-infra local section: OpenTofu `fmt`/`validate` + plan-gate, Actions `env:`+`jq` no-inline-`${{ }}`, scanner DoD ruff/bandit/pytest + pinned tools, and the must-not-be-committed table) and the `.ai/` cursor. **Correction to the plan as written:** the cursor is **`.ai/next-steps.md` only** — loop-orchestrator git-ignores `.ai/state.json` (it is session-local scratch; `next-steps.md` is what travels), so this repo ignores it too rather than committing one. Still open in T1: the required-status-checks list, deferred to S0's final step.
  - **Target Files:** the ruleset (`gh api repos/glunk-works/bounty-infra/rulesets` — needs admin), `CLAUDE.md`, `.ai/next-steps.md`, `.gitignore` (ignore `.ai/state.json`), `docs/hardening_roadmap.md`, `sprints/S0_governance_hardening/sprint_plan.md`.
  - **Acceptance:** ✅ `main` requires a PR to merge and rejects force-push/deletion — verified via `GET /repos/glunk-works/bounty-infra/rules/branches/main`; ✅ `CLAUDE.md` + `.ai/` cursor exist and point at the shared conventions; ⬜ the required-status-checks list is applied once T2/T3's checks exist (deferred to S0's final step) and matches the T3 taxonomy.

- **Task 2: Gated OpenTofu deploy (#9)** — ✅ **DONE 2026-07-21; plan path verified live, apply path not yet exercised.** `plan-infra.yml` (job `tofu-plan`) + `deploy-infra.yml` (job `apply`, `environment: production`); the `production` Environment exists with required reviewer `Seuss27`, deployments restricted to `main`.
  - **Verified 2026-07-21:** `tofu-plan` green on PR #25 with `tofu plan exit code: 0` — a **no-op plan against real state**, which is this task's acceptance criterion. Confirmed in AWS that `github-actions-bounty-infra-plan` trusts only `:pull_request` and carries just `glunk-works-bounty-infra-plan-readonly` + `bounty-infra-plan-state-read`, and that the apply role trusts both `:ref:refs/heads/main` and `:environment:production`.
  - **NOT yet exercised: the apply path.** No `infra/**` file changed, so merging #25 does not trigger an apply. The environment approval gate and the `environment:production` subject are both unproven until the first real infra change — or a deliberate `workflow_dispatch` of `deploy-infra.yml` after merge, which is the cheaper test (a no-op apply).
  - **RESOLVED BLOCKER — the plan job needed its own read-only identity.** The first live run failed `403 Access denied: OIDC subject not allowed`: on `push` the GitHub OIDC subject is `repo:glunk-works/bounty-infra:ref:refs/heads/main`, but on `pull_request` it is `repo:glunk-works/bounty-infra:pull_request`, which the existing Infisical identity does not allow. **Resolved by decision, not by widening that identity** — a plan is a read operation and a PR is unreviewed code (workflow changes in a PR take effect for `pull_request` runs), so broadening the apply identity would make *opening a PR* grant apply-capable AWS credentials with no approval, re-opening #9 through the side door. Required setup, in order:
    1. **AWS — done as code in `glunk-works/global-bootstrap` PR #1** (`plan_roles.tf`), *not* by hand. **Correction to an earlier note in this file:** the GitHub OIDC roles are **not** bootstrapped out-of-band — `global-bootstrap` generates one per project from `var.projects`, and its trust condition (`sub = repo:<org>/<repo>:ref:refs/heads/main`) is the exact cause of the 403. That PR adds an opt-in second role `github-actions-bounty-infra-plan` trusted only from the `:pull_request` subject, with a **read-only mirror of the workload policy** (deliberately not the managed `ReadOnlyAccess`, which would grant `s3:GetObject` on the findings archive), state `GetObject`/`ListBucket` on the `bounty-infra/` prefix only, no DynamoDB lock verbs (plan runs `-lock=false`), and an explicit `Deny` on the findings bucket. No KMS grant needed — `backend.tf` sets `encrypt = true` with no `kms_key_id`, so state is SSE-S3. **Owner action: review and `tofu apply` that PR**, then take the ARN from the new `github_actions_plan_role_arns` output.
    2. **Infisical:** a second machine identity whose OIDC subject allowlist is exactly `repo:glunk-works/bounty-infra:pull_request`, with read access at `/bounty-infra` (`prod`) to `AWS_REGION`, `TF_STATE_BUCKET`, `TF_STATE_LOCK_TABLE`, `FINDINGS_BUCKET_NAME`, `KMS_KEY_ARN`, and a **new** secret `AWS_PLAN_ROLE_ARN` holding the role from step 1. It must **not** be able to read `AWS_OIDC_ROLE_ARN`.
    3. **GitHub:** repo variable `PLAN_IDENTITY_ID` = the new identity's id (mirrors the existing `vars.IDENTITY_ID`, which stays bound to the apply path).
  - **The existing `IDENTITY_ID` must stay scoped to the `main` ref subject** for `pull_request`. Do not "fix" a PR-time 403 by adding `pull_request` to it.
  - **(v) A GitHub Environment CHANGES the OIDC subject — found late, and it would have broken the apply.** Per GitHub's OIDC reference the subject filters are ordered: a job referencing an Environment presents `repo:<org>/<repo>:environment:<name>` **instead of** the branch subject (and instead of `pull_request`). Adding `environment: production` to the apply job therefore silently invalidated its credentials on **both** hops — the Infisical identity's subject allowlist *and* the AWS role's trust condition — each of which pinned to `:ref:refs/heads/main`. Fixed by **appending**, never replacing: `extra_oidc_subjects` in `global-bootstrap` (PR #1) and a second entry on the Infisical identity, because `build-image.yml` and `run-scan.yml` share that role, reference no Environment, and still present the branch subject. **Generalise this:** changing a job's trigger or adding an Environment is a change to its *identity*, not just its scheduling.
  - **Four deviations from the plan as written, all recorded here:**
  - **(i) `plan-infra.yml` carries NO `paths:` filter.** As written this task said "a plan job on PRs touching `infra/**`", which contradicts **T3(a)**: a path-filtered workflow cannot host a required check (a `docs/`-only PR never triggers it, so the check sits pending forever). The job runs on **every** PR and decides internally, via a merge-base `git diff`, whether there is anything to plan — a **step-level** `if:`, so the job always reports a conclusion. No new third-party action was introduced to do the detection (#12).
  - **(ii) `fmt -check`/`tflint` are NOT in this task.** They are credential-free and belong to T3's `tofu-validate` job; duplicating them in the credentialed plan job buys nothing. **T2's acceptance is met jointly by T2 + T3** — do not read `tofu-plan` alone as satisfying "fmt/validate/tflint/plan" (`tofu plan` does imply `validate`).
  - **(iii) `prevent_self_review: false` on the Environment** — the exact parallel to T1's `required_approving_review_count: 0` finding. Single collaborator, so a self-review ban would make every apply permanently unapprovable. The approval is still an explicit, logged, out-of-band human action.
  - **(iv) The apply re-plans instead of consuming the PR's plan file.** BI-D4 forbids uploading `tfplan.bin` (it embeds the account ID, bucket names and ARNs, and artifacts are world-readable on a public repo), so there is no artifact to hand off. The reviewer approves the **change**; the apply is re-derived at execution time. Accepted TOCTOU window: state could drift between plan and approval.
  - **Description:** Replace `deploy-infra.yml`'s `push→apply -auto-approve` with two gates. **(a)** A `tofu plan` job on **PRs touching `infra/**`** — `tofu fmt -check`, `tofu validate`, `tflint`, then `tofu plan` with output posted (PR comment or uploaded artifact) so the reviewer reads the plan. **(b)** An **apply job on merge-to-main** that targets a protected GitHub **`production` Environment** (required reviewer); `tofu apply -auto-approve` runs **only after** the environment approval. Create the `production` Environment with required reviewers.
  - **Target Files:** `.github/workflows/deploy-infra.yml` (split into plan-on-PR + apply-on-merge), possibly a new `plan-infra.yml`; the GitHub `production` Environment (settings/API).
  - **Acceptance:** a PR touching `infra/**` runs fmt/validate/tflint/plan and surfaces the plan; merge to main does **not** apply until a required reviewer approves the `production` environment; no path applies infra without a visible plan + an approval. Existing deploys still succeed through the new flow (verify against a no-op plan).

- **Task 3: Non-bypassable CI (#8)**
  - **Description:** Make `ci.yml` unskippable and make the ruleset that enforces it self-auditing. Five parts:
  - **(a) Drop the `paths:` filters entirely.** `ci.yml` is currently `pull_request` on `src/**` only. Path filters and required checks **deadlock**: a PR touching only `docs/` never triggers the workflow, so a required check sits pending forever and the PR can never merge. Widening the filter list does not fix this — only removing it does. The repo is small; run CI unconditionally rather than adding a paths-filter action (a new unpinned third-party dependency, which #12 is open about).
  - **(b) Adopt the job-id-as-check-context convention.** loop-orchestrator's CI jobs carry **no `name:` override**, so the job id *is* the check context (`lint`, `test`, …). bounty-infra currently sets `name: "Lint & Test"` / `name: "Hatch Build & Validate"`, which would force required checks to be those display strings and re-opens the hazard that renaming a job silently un-requires the gate. **Drop the `name:` overrides; rename the jobs to `lint` / `test`.**
  - **(c) Add an infra job** (`tofu fmt -check`, `validate`, `tflint`). Use **`tofu init -backend=false`** for validate so it needs no AWS credentials; only T2's `plan` requires real creds. Pin `tflint` — do not introduce a new `@latest` tool while #12 is open.
  - **(d) Gate `build-image.yml` (the missing half of #8).** It triggers `push → main` on `src/**` with **no dependency on `ci.yml`**, pushes `:latest` to an ECR repo declared `image_tag_mutability = "MUTABLE"` (`infra/main.tf:67`), and the task definition runs `…:latest` (`infra/main.tf:148`). So merging any `src/**` change **silently replaces the code running in production Fargate** — no CI, no plan, no approval. That is #9's shape applied to code, and gating the infra apply (T2) while leaving this open would be a false gate. Make it depend on CI green and deploy by immutable `:${{ github.sha }}` rather than mutable `:latest`.
  - **(e) Port loop-orchestrator's `ruleset-drift.yml`.** Every required check blocks a merge *only because the ruleset says so*; if the ruleset is weakened or deleted, nothing else notices — which is exactly how its total absence went unseen here until 2026-07-21. Re-read `/rules/branches/main` on a cron + `workflow_dispatch` and fail loudly when rule types or contexts go missing. **Deliberately NOT a required check and NOT a job in `ci.yml`** (loop-orchestrator FD2): a required check is required only because the ruleset says so, so making the drift guard itself required would un-require it the instant the ruleset it watches is deleted — silently, on the exact failure it exists to catch. It gates nothing and is gated by nothing.
    - Optionally reconcile the `ci.yml` py-3.11-validate / py-3.14-package drift (the closed #16) if trivial — note the drift is still present despite `41a9f85` ("align Python runtime with CI"), so confirm whether #16 was closed prematurely.
  - **Target Files:** `.github/workflows/ci.yml` (drop paths, rename jobs, add infra job), `.github/workflows/build-image.yml` (CI dependency + sha tag), a new `.github/workflows/ruleset-drift.yml`, `infra/main.tf` (task-definition image tag), the ruleset required-checks (finalized here / with T1).
  - **Check taxonomy (BI-D3 convergence target).** loop-orchestrator requires `lint`, `format-check`, `test`, `secrets-scan`, `dependency-audit`, `sbom`, `pr-title`, `architect-review`. bounty-infra's required list should converge on that set plus its local extension (`tofu-validate`, `tofu-plan`). S0 lands `lint`, `test`, `tofu-validate`, `tofu-plan`; **`secrets-scan` / `dependency-audit` / `sbom` / `pr-title` / `architect-review` are not yet adopted here and are an OPEN sequencing decision** (see Out of scope).
  - **Acceptance:** CI runs on **every** PR regardless of path; job ids equal check contexts; `lint` + `test` + `tofu-validate` + the `tofu plan` check are **required** on `main`; **no merge can ship a container image that has not passed CI**, and the deployed image is sha-pinned, not `:latest`; `ruleset-drift.yml` passes on the real ruleset and demonstrably fails when a rule is removed (test it by dispatching against a deliberately weakened copy, not by weakening the live ruleset).

- **Task 4: `run-scan.yml` injection fix (#6) + drop unused token (#10)**
  - **Description:** Rewrite the *Trigger Scan Task* step so **no `${{ github.event.inputs.* }}` is interpolated inline into a `run:` shell block.** Pass all four inputs via an `env:` block; build the ECS `--overrides` JSON with **`jq -n --arg`**, referencing quoted `"$TARGET_DOMAIN"` etc. Add a **strict hostname regex** validation on `target_domain` before use (reject malformed input early — a structural check, distinct from S1's full RoE scope in the scanner). **Remove the unused `GITHUB_TOKEN` env var and the `issues: write` permission** (#10 — the scanner uploads to S3 and makes no GitHub API call). **Verify with a `'`-containing `target_domain`** that it is rejected/neutralized, not executed.
  - **Target Files:** `.github/workflows/run-scan.yml`.
  - **Acceptance:** dispatch inputs are consumed via `env:` + `jq --arg`, never inline `${{ }}` in `run:`; a `target_domain` containing a single quote does not break out (verified); `target_domain` is hostname-validated; `GITHUB_TOKEN`/`issues: write` are gone; the workflow still launches a scan correctly for a valid domain. **This clears the #6 precondition on loop-orchestrator #18** (its `seed`/`token` inputs then ride this safe pattern).

**Security Considerations:** S0 is entirely defensive. The three High findings (#6/#8/#9) are the "green for the wrong reason" class — an unprotected `main`, a CI that doesn't run on the delivery path, and an auto-apply with no plan. Fixing them **first** is what makes S1/S2's security fixes trustworthy (they land through a real gate). **#6 severity is anticipatory, not live** (qualified 2026-07-21): `workflow_dispatch` requires repo **write** access and there is a single collaborator, so it is not currently an external privilege-boundary crossing — a principal who can dispatch could equally commit a malicious workflow. It becomes genuinely High the moment **#18** makes the workflow machine-dispatchable from loop-orchestrator and `target_domain` stops being operator-typed. Ordering is unchanged (T4 still ships before #18) but S0 is **not** firefighting. Full pre-remediation detail is preserved in private draft advisories `GHSA-59j8-c4rc-2jf4` (#6), `GHSA-pf9q-vx7g-f8gr` (#7), `GHSA-p3hr-h7cq-xp5m` (#13), to publish as each sprint closes. **T4 has an in-house reference implementation:** loop-orchestrator's `ruleset-drift.yml` is the exact safe shape — `set -euo pipefail`, every value through `env:`, all JSON via `jq`, no inline `${{ }}` in the `run:` block. No new secret, no new external surface.

**Risks & Blockers:**
- **Admin scope needed** for the ruleset (T1) and the `production` Environment (T2). If unavailable, T1/T2 can't complete — surface early.
- **The apply-gate must not break live infra delivery.** Test the new plan/apply split against a **no-op plan** before relying on it; a mis-split could strand `main`→infra.
- **Required-checks sequencing** — never require a check that doesn't exist yet (it strands the requirement). Apply the required-status-checks list only after T2/T3's jobs have run once (finalize at S0's end).
- **#6 verification is behavioral, not just structural** — prove a quote-containing input is neutralized; don't just eyeball the `jq` rewrite.
- **Method discipline from here:** every S0 change lands via a reviewed PR (branch cut from `main`, never pushed to `main`); never merge your own PR; the human's merge is the approval.
